#!/usr/bin/node
// A script that prints three strings on different lines

let stringLines = ["C is fun", "Python is cool", "JavaScript is amazing"]

for (let i = 0; i < stringLines.length; i++) {
	console.log(stringLines[i]);
}
